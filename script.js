$(document).ready(function(){

	$(function() {
	    $('.center').css({
	        'position' : 'absolute',
	        'left' : '50%',
	        'top' : '50%',
	        'margin-left' : -$('.center').outerWidth()/2,
	        'margin-top' : -$('.center').outerHeight()/2
	    });
	});
});